apiVersion: v1
kind: Secret
metadata:
    name: {{ .Values.fullnameOverride }}
    labels:
        app: {{ .Values.fullnameOverride }}
        name: {{ .Values.fullnameOverride }}
type: Opaque
data:
  {{- range $key, $val := .Values.env.secret }}
  {{ $key }}: {{ tpl $val $ | b64enc }}
  {{- end}}
